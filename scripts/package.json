{
  "name": "@israeli-law-rag/scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Data processing scripts for Israeli Law RAG Chatbot",
  "type": "module",
  "scripts": {
    "verify-qdrant": "tsx src/verify-qdrant-cluster.ts",
    "create-collection": "tsx src/create-collection.ts",
    "create-indexes": "tsx src/create-indexes.ts",
    "process-pdfs": "tsx src/process-pdfs.ts",
    "cluster-topics": "tsx src/cluster-topics.ts",
    "migrate": "tsx src/run-migrations.ts",
    "migrate:up": "tsx src/run-migrations.ts up",
    "migrate:down": "tsx src/run-migrations.ts down",
    "migrate:status": "tsx src/run-migrations.ts status",
    "migrate:reset": "tsx src/run-migrations.ts reset",
    "migrate:refresh": "tsx src/run-migrations.ts refresh"
  },
  "dependencies": {
    "@israeli-law-rag/lib": "*"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "typescript": "^5.4.2"
  }
}
